{
	    "tasks":[

	        {
	            "type": "process",
	            "label": "build the Complier",
	            "command": "./build.sh",
	            "args": [],
	            "options": {
	                "cwd": "${workspaceFolder}"
	            },
	            "group": {
	                "kind": "build",
	                "isDefault": true
	            },
	            "detail": "Task generated by Debugger.",
	            "isBuildCommand": true
	        },
	        {
	            "type": "process",
	            "label": "run current .tq",
	            "command": "${workspaceFolder}/bin/Complier",
	            "args": ["${file}"],
	            "options": {
	                "cwd": "${workspaceFolder}"
	            },
	            "dependsOn": ["build the Complier"],
	            "problemMatcher": []
	        }
	    ],
	    "version": "2.0.0"
	}
