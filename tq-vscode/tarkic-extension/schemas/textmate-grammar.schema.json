{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "TextMate Grammar (minimal)",
	"description": "Minimal schema used to avoid remote/vscode:// schema loading issues. It is intentionally permissive.",
	"type": "object",
	"additionalProperties": true
}
